# Persistence MCP Server
# Exposes tools: create_investment(), get_investment(), update_investment_status(),
#                get_alert_state(), update_alert_state(), log_event(),
#                get_price_cache(), set_price_cache()
# Owns: SQLite database and all schema knowledge
# Status: TO BUILD
